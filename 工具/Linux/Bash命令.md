理解了！你是在学习 C++ 和虚幻引擎开发，现在你要学习 Bash 命令。我来给你介绍一下 Bash 命令的核心概念，采用你的笔记风格——从底层原理到实际应用。

## 📚 Bash 命令介绍

### **核心摘要**
Bash 是 Unix/Linux/macOS 的标准 shell，通过命令行允许用户与操作系统交互、执行程序、管理文件和系统资源。

---

### **详细分析**



#### **2. 底层原理**

**Bash 的执行流程：**

```
用户输入 → 词法分析 → 语法分析 → 环境变量扩展 → 命令查找 → 进程创建 → 命令执行 → 返回状态码
```

**核心概念：**

| 概念             | 解释                             | 示例                               |
| -------------- | ------------------------------ | -------------------------------- |
| **命令**         | 可执行程序或内置函数                     | `ls`, `cd`, `echo`               |
| **参数/选项**      | 命令的输入                          | `ls -la /home`                   |
| **标准输入/输出/错误** | stdin(0), stdout(1), stderr(2) | `cmd > output.txt 2>&1`          |
| **管道**         | 连接命令，前一个的输出是后一个的输入             | `cat file.txt \| grep "pattern"` |
| **重定向**        | 改变数据流向                         | `cmd > file`, `cmd < input.txt`  |
| **环境变量**       | 全局变量，影响程序行为                    | `$HOME`, `$PATH`, `$USER`        |

---

### **3. 常用 Bash 命令分类**

#### **📁 文件和目录操作**

```bash
# 列出文件
ls              # 列出当前目录
ls -la          # 详细列表（包括隐藏文件）
ls -R           # 递归列出所有子目录

# 目录导航
pwd             # 打印当前工作目录
cd /path/to/dir # 改变目录
cd ..           # 返回上一级
cd ~            # 返回主目录
cd -            # 返回上一个目录

# 创建和删除
mkdir dir_name         # 创建目录
mkdir -p a/b/c         # 递归创建目录
rmdir dir_name         # 删除空目录
rm file.txt            # 删除文件
rm -r dir_name         # 递归删除目录（危险！）
rm -f file.txt         # 强制删除（不提示）

# 复制和移动
cp source.txt dest.txt           # 复制文件
cp -r source_dir dest_dir        # 递归复制目录
mv old_name.txt new_name.txt     # 移动/重命名文件

# 查看文件内容
cat file.txt           # 显示文件全部内容
head -n 10 file.txt    # 显示前 10 行
tail -n 10 file.txt    # 显示最后 10 行
less file.txt          # 分页查看（q 退出）
```

#### **🔍 搜索和查找**

```bash
# 搜索文件
find . -name "*.cpp"           # 在当前目录找所有 .cpp 文件
find /path -type f -size +1M   # 找大于 1MB 的文件

# 搜索文本内容
grep "pattern" file.txt        # 在文件中查找文本
grep -r "pattern" .            # 递归查找
grep -i "pattern" file.txt     # 不区分大小写
grep -n "pattern" file.txt     # 显示行号

# 定位命令
which gcc                      # 找到 gcc 命令的位置
whereis python                 # 找到 python 的所有位置
```

#### **📝 文本处理**

```bash
# 输出和写入
echo "Hello World"                  # 输出文本
echo "text" > file.txt              # 覆盖写入文件
echo "text" >> file.txt             # 追加到文件

# 文本处理工具
sed 's/old/new/g' file.txt         # 替换文本（sed）
awk '{print $1}' file.txt          # 按列处理文本（awk）
cut -d',' -f1 file.txt             # 按分隔符切割（cut）
sort file.txt                      # 排序行
uniq file.txt                      # 去重
wc -l file.txt                     # 统计行数
```

#### **🔧 权限和所有权**

```bash
# 查看权限
ls -l file.txt              # 显示文件权限

# 修改权限（rwx = 4+2+1 = 7）
chmod 755 script.sh         # 所有者读写执行，其他只读执行
chmod +x script.sh          # 增加执行权限
chmod u+w file.txt          # 所有者增加写权限

# 修改所有权
chown user:group file.txt   # 改变所有者
```

#### **⚙️ 进程和系统**

```bash
# 查看进程
ps aux                      # 显示所有运行的进程
ps aux | grep gcc           # 查找 gcc 进程
top                         # 实时查看进程（q 退出）
kill PID                    # 终止进程
kill -9 PID                 # 强制终止进程

# 系统信息
uname -a                    # 系统信息
df -h                       # 磁盘使用情况
du -sh dir/                 # 目录大小
free -h                     # 内存使用情况
```

#### **🔗 管道和重定向**

```bash
# 管道（|）：连接两个命令
cat file.txt | grep "error" | wc -l    # 文件中 error 出现次数

# 输出重定向（>）
command > output.txt        # 输出到文件
command >> output.txt       # 追加输出

# 输入重定向（<）
command < input.txt         # 从文件读取输入

# 错误重定向（2>）
command 2> error.log        # 错误输出到文件
command > output.txt 2>&1   # 所有输出（标准+错误）都到文件
```

---

### **4. 脚本编写基础**

#### **Bash 脚本结构**

```bash
#!/bin/bash                    # Shebang：指定解释器

# 变量定义
name="value"
count=10
array=(1 2 3 4 5)

# 条件判断
if [ $count -gt 5 ]; then
    echo "Count is greater than 5"
elif [ $count -eq 5 ]; then
    echo "Count equals 5"
else
    echo "Count is less than 5"
fi

# 循环
for i in {1..5}; do
    echo "Iteration $i"
done

# While 循环
while [ $count -gt 0 ]; do
    echo $count
    count=$((count - 1))
done

# 函数定义
function greet() {
    echo "Hello, $1!"
}

greet "World"
```

---

### **5. 实际应用场景（与你的学习相关）**

#### **C++ 编译工作流自动化**

```bash
#!/bin/bash

# 编译 C++ 项目
g++ -std=c++17 -O2 -o program main.cpp -lstdc++

# 运行程序
./program

# 清理构建文件
rm -f *.o program
```

#### **搜索代码中的 虚函数/CDO 相关内容**

```bash
# 在整个项目中搜索虚函数定义
grep -r "virtual" --include="*.cpp" --include="*.h" .

# 在 UE 项目中查找 CDO 相关代码
grep -r "CDO\|GetDefaultObject" --include="*.cpp" .
```

---

