# 导览模块交互流程图

> **项目**: YiTiJi_Move
> **模块路径**: `Assets/c#/SystemLayer/Guide/`
> **生成时间**: 2026-02-18

---

## 一、模块总览图

展示导览模块各组件之间的关系和数据流向。

```mermaid
graph TB
    Voice["语音识别"] --> CR["CommandRouter"]
    Touch["触屏操作"] --> MUF["MainUIForm"]

    CR --> NWG["NetWorkGuide"]
    CR --> EFM["ExitFlowManager"]

    NWG <--> Server["导览服务"]
    NWG --> GM["GuideMgr"]

    GM --> GUF["GuideUIForm"]
    GM --> ASM["AppStateManager"]

    EFM --> ASM
    EFM --> NWG

    GUF --> MUF
    ASM --> MUF
    ASM --> QPF["QiPaoUIForm"]
```

### 核心组件说明

| 组件 | 文件 | 职责 |
|------|------|------|
| **GuideMgr** | `GuideMgr.cs` | 导览管理器 - 处理导览响应事件、方向判断、点位到达 |
| **NetWorkGuide** | `NetWorkGuide.cs` | WebSocket通信 - 发送/接收导览指令 |
| **GuideUIForm** | `GuideUIForm.cs` | 导览界面 - 管理UI展示、打字机效果、音视频播放 |
| **CommandRouter** | `CommandRouter.cs` | 指令路由 - 语音识别文本→业务指令 |
| **ExitFlowManager** | `ExitFlowManager.cs` | 退出流程 - 不同状态的退出策略 |
| **ExitReason** | `ExitReason.cs` | 退出原因枚举 |

---

## 二、完整交互流程图

从用户触发导览到结束的全流程。

```mermaid
sequenceDiagram
    participant User as 用户
    participant Voice as 语音识别
    participant CR as CommandRouter
    participant NWG as NetWorkGuide
    participant Server as 导览服务
    participant GM as GuideMgr
    participant GUF as GuideUIForm
    participant ASM as AppStateManager

    User->>Voice: 语音指令"开始导览"
    Voice->>CR: CheckInstruct(msg)
    CR->>NWG: AutoDaoLan() / HandDaoLan()
    NWG->>Server: 发送导览请求
    NWG->>ASM: TransitionTo(Moving)

    Server-->>NWG: 方向判断结果(event=7)
    NWG-->>GM: OnGuideResponse
    GM->>NWG: Move()

    Server-->>NWG: 到达点位(event=8)
    NWG-->>GM: OnGuideResponse
    GM->>GUF: OpenGuideUI(stationId)
    GM->>ASM: SetNavigating(false)
    GUF->>ASM: TransitionTo(Guiding)

    GUF->>GUF: StartPlay(pointId)
    Note over GUF: 播放文本/图片/视频

    GUF->>GUF: CloseGuideUI()
    GUF->>ASM: TransitionTo(Previous)
```

---

## 三、状态机流程图

AppState 状态转换关系（基于 AppStateManager.cs）。

```mermaid
stateDiagram-v2
    [*] --> Idle: 初始化

    Idle --> Moving: Move指令/GetDirection
    Idle --> Talking: 语音唤醒
    Idle --> Guiding: OpenGuideUI(stationId)

    Moving --> Idle: 到达终点(direction=2)
    Moving --> Guiding: 到达普通点位(event=8)

    Talking --> Idle: GoBack返回
    Talking --> Guiding: OpenGuideUI打开导览

    Guiding --> Idle: CloseGuideUIAndTask
    Guiding --> Talking: 语音唤醒暂停

    Idle --> [*]: ExecuteBackToLogon退出

    note right of Idle
        IsNavigating: false
        IsGoodByeing: false
        CurrentGuideMode: Manual/Auto
    end note

    note right of Moving
        IsNavigating: true
        播放动画: YT_yddh-02(1-57)
        循环调用 Move()
    end note

    note right of Guiding
        IsNavigating: false
        播放动画: YT_yddh-02(181-234)
        StartPlay(pointId)
    end note
```

### 状态说明

| 状态 | 含义 | 进入条件 | 退出条件 | 关键标志 |
|------|------|----------|----------|----------|
| **Idle** | 空闲待命 | 初始化/到达终点/关闭UI | Move指令/语音唤醒/到达点位 | `IsNavigating=false` |
| **Moving** | 移动中 | `GetDirection()`返回direction=0/1 | 到达终点/到达点位 | `IsNavigating=true` |
| **Talking** | 对话中 | 语音唤醒触发 | GoBack返回/打开导览UI | 对话界面显示 |
| **Guiding** | 导览播放中 | `HandleArriveNormalStation()` | `CloseGuideUIAndTask()` | `IsNavigating=false` |

### 状态转换触发点

| 触发方法 | 源状态 | 目标状态 | 文件位置 |
|----------|--------|----------|----------|
| `GuideMgr.HandleMoveRight()` | Idle | Moving | GuideMgr.cs:122 |
| `GuideMgr.HandleReachDestination()` | Moving | Idle | GuideMgr.cs:131 |
| `GuideMgr.HandleArriveNormalStation()` | Moving | Guiding | GuideMgr.cs:214 |
| `GuideMgr.OpenGuideUI()` | Talking/Idle | Guiding | GuideMgr.cs:265 |
| `GuideUIForm.CloseGuideUIAndTask()` | Guiding | Idle | GuideUIForm.cs:145 |

### GuideMode 模式说明

| 模式 | 枚举值 | 触发方法 | 行为 |
|------|--------|----------|------|
| **None** | 0 | 初始化 | 无导览模式 |
| **Auto** | 1 | `NetWorkGuide.AutoDaoLan()` | 自动播放各点位 |
| **Manual** | 2 | `NetWorkGuide.HandDaoLan()` | 手动控制导览 |

---

## 四、导览模式流程图

手动/自动/全局三种导览模式的切换和行为。

```mermaid
graph LR
    subgraph Manual["手动导览 Manual"]
        M1["用户语音指令"]
        M2["上一点位/下一点位"]
        M3["手动控制移动"]
    end

    subgraph Auto["自动导览 Auto"]
        A1["自动播放介绍"]
        A2["介绍完自动移动"]
        A3["循环到下一点位"]
    end

    subgraph Global["全局导览 Global"]
        G1["从第一个点位开始"]
        G2["遍历所有点位"]
        G3["自动返回起点"]
    end

    Manual -->|切换自动导览| Auto
    Auto -->|切换手动导览| Manual
    Manual -->|全局导览| Global
    Global -->|结束| Manual
```

### 导览模式详细说明

#### 手动导览 (HandDaoLan)
```mermaid
sequenceDiagram
    participant User as 用户
    participant CR as CommandRouter
    participant NWG as NetWorkGuide
    participant Server as 服务器

    User->>CR: "介绍下一点位"
    CR->>NWG: NextPoint()
    NWG->>NWG: CloseGuideUI()
    NWG->>NWG: GetDirection()
    NWG->>Server: 发送NextPoint请求
    NWG->>NWG: SetGuideMode(Manual)
```

#### 自动导览 (AutoDaoLan)
```mermaid
sequenceDiagram
    participant User as 用户
    participant CR as CommandRouter
    participant NWG as NetWorkGuide
    participant MUF as MainUIForm

    User->>CR: "切换自动导览"
    CR->>NWG: AutoDaoLan()
    NWG->>NWG: CloseGuideUI()
    NWG->>MUF: ShowGuideAutoUI()
    Note over NWG: 自动循环播放各点位
```

#### 全局导览 (AutoGlobalDaoLan)
```mermaid
sequenceDiagram
    participant User as 用户
    participant CR as CommandRouter
    participant NWG as NetWorkGuide
    participant MUF as MainUIForm

    User->>CR: "全局导览"
    CR->>NWG: AutoGlobalDaoLan()
    NWG->>NWG: CloseGuideUI()
    NWG->>MUF: ShowGuideAutoUI()
    NWG->>MUF: OnClickGoBack()
    Note over NWG: 从头遍历所有点位
```

---

## 五、UI播放流程图

GuideUIForm 中文本/图片/视频内容的播放和同步机制。

```mermaid
graph TD
    Start["StartPlay(pointId)"] --> Init["初始化StageList"]
    Init --> Loop["遍历StageList"]

    Loop --> CheckType{"内容类型?"}

    CheckType -->|text| Text["创建文本预制体"]
    CheckType -->|image| Image["创建图片预制体"]
    CheckType -->|video| Video["创建视频预制体"]

    Text --> TypeWriter["打字机效果<br/>WalkAsync"]
    Text --> AudioTask["音频播放任务<br/>不阻塞"]

    Image --> LoadImg["异步加载图片"]
    LoadImg --> BlurBg["高斯模糊背景"]
    BlurBg --> WaitPic["等待5秒或点击"]

    Video --> PrepareVideo["准备视频缩略图"]
    PrepareVideo --> PlayVideo["打开视频播放器"]
    PlayVideo --> WaitVideo["等待播放完成"]

    TypeWriter --> NextStage["标记完成<br/>进入下一Stage"]
    WaitPic --> NextStage
    WaitVideo --> NextStage

    NextStage --> CheckEnd{"所有Stage完成?"}
    CheckEnd -->|否| Loop
    CheckEnd -->|是| Close["关闭导览UI"]
```

### 内容类型处理详解

#### 文本播放流程
```mermaid
sequenceDiagram
    participant MC as MainCoroutine
    participant Text as RTLTextMeshPro
    participant Audio as AudioMgr
    participant Scroll as AutoScroller

    MC->>Text: 创建文本预制体
    MC->>MC: 计算文本高度

    par 并行执行
        MC->>Audio: PlayAudioWhenConditionAsync()
        Note over Audio: 等待前一Stage完成
        Audio->>Audio: 加载音频(预加载/备用)
        Audio->>Scroll: 设置滚动速度
        Audio->>Audio: 播放音频
    and
        MC->>Text: WalkAsync() 打字机效果
        loop 逐字显示
            Text->>Text: 添加字符
            Text->>Text: 延迟0.05秒
        end
    end

    Audio-->>MC: OnAudioFinished
    MC->>MC: StageIdDict[id] = true
```

#### 图片播放流程
```mermaid
sequenceDiagram
    participant MC as MainCoroutine
    participant Img as Image组件
    participant Pic as PicUIForm

    MC->>Img: 创建图片预制体
    MC->>MC: LoadImageAsync()
    MC->>MC: 下载图片
    MC->>MC: 裁剪+高斯模糊背景
    MC->>MC: 缓存到_cacheSpriteDict

    MC->>MC: PlayPicWhenConditionAsync()
    Note over MC: 等待前一Stage完成

    alt 用户点击图片
        MC->>Pic: OpenPicUIForm()
        Pic->>Pic: 全屏显示
        Pic-->>MC: 关闭
    else 等待5秒
        MC->>MC: WaitWithPicUIFormPause()
    end

    MC->>MC: OnPicFinishedHandler()
    MC->>MC: StageIdDict[id] = true
```

#### 视频播放流程
```mermaid
sequenceDiagram
    participant MC as MainCoroutine
    participant VP as VideoPlayer
    participant VUF as VideoUIFormNew

    MC->>VP: 创建视频预制体
    MC->>VP: 设置视频URL
    MC->>VP: 创建RenderTexture

    MC->>MC: PrepareVideoThumbnailAsync()
    VP->>VP: Prepare()
    VP->>VP: 播放0.3秒截图
    VP->>VP: 高斯模糊缩略图

    MC->>MC: PlayVideoWhenConditionAsync()
    Note over MC: 等待前一Stage完成

    MC->>VUF: OpenUIForm()
    VUF->>VUF: PlayVideo(url, id)
    VUF-->>MC: OnVideoFinished

    MC->>MC: StageIdDict[id] = true
```

### 暂停/恢复机制

```mermaid
sequenceDiagram
    participant Voice as 语音唤醒
    participant GUF as GuideUIForm
    participant UTC as UniTaskGlobalController
    participant Audio as AudioMgr

    Voice->>GUF: AwakeRecord("唤醒")
    GUF->>UTC: PauseAllUniTask()
    GUF->>GUF: Hiding()
    GUF->>Audio: StopAudio()
    Note over GUF: 暂停所有播放任务

    Voice->>GUF: NoTouchInput("恢复")
    GUF->>GUF: ReStart()
    GUF->>UTC: ResumeAllUniTask()
    GUF->>GUF: 找到未完成的Stage
    GUF->>GUF: 滚动到对应位置
    GUF->>GUF: 继续播放音频/视频
    GUF->>GUF: Display()
```

---

## 六、退出流程图

基于 ExitFlowManager.cs 的退出处理策略。

```mermaid
graph TD
    Start["触发 ExecuteBackToLogon(reason)"] --> SetState["SetGoodByeing(true)"]
    SetState --> CloseUI["CloseGuideUIAndTask(false)"]
    CloseUI --> CheckReason{"退出原因?"}

    CheckReason -->|Goodbye| Pre1["等待 2000ms<br/>MainUIForm.GoBack()"]
    CheckReason -->|Timeout| Pre2["关闭图片UI<br/>CloseUIForms(PIC_UIFORM)"]
    CheckReason -->|其他| Pre3["无特殊前置处理"]

    Pre1 --> Core["BackToLogon(reason)"]
    Pre2 --> Core
    Pre3 --> Core

    Core --> Step1["播放退场动画<br/>PlayAnimOld(YT_tc_laoyangtuo)"]
    Step1 --> Step2["禁用语音唤醒<br/>isWakeupEnabled = false"]
    Step2 --> Step3["显示tcObj<br/>tcObj.SetActive(true)"]
    Step3 --> Step4["WaitAndMoveOut()<br/>异步等待"]
    Step4 --> Step5["隐藏气泡<br/>HideQiPao()"]
    Step5 --> Step6["显示主界面遮罩<br/>MainMask.SetActive(true)"]
    Step6 --> Step7["设置导览模式<br/>SetGuideMode(Manual)"]

    Step4 -.延迟等待.-> Wait["等待 playerGetOutTime"]
    Wait --> Finish["Finish()<br/>发送退出指令"]
    Finish --> FinalAnim["播放移动动画<br/>YT_yddh-02(57-86)"]
    FinalAnim --> Hide["隐藏tcObj"]
    Hide --> Done["退出完成"]
```

### 退出原因枚举 (ExitReason.cs)

| 原因 | 枚举值 | 触发场景 | 前置处理 | 触发位置 |
|------|--------|----------|----------|----------|
| **Timeout** | 0 | 无操作超时 | 关闭图片UI | 超时检测逻辑 |
| **Goodbye** | 1 | 用户说"再见" | 等待2秒+GoBack | CommandRouter.cs:63 |
| **Manual** | 2 | 点击退出按钮 | 无 | UI按钮事件 |
| **Error** | 3 | 错误导致退出 | 无 | 异常处理 |
| **GuideFinished** | 4 | 导览结束 | 无 | 导览完成检测 |

### 核心退出方法调用链

```mermaid
sequenceDiagram
    participant CR as CommandRouter
    participant EFM as ExitFlowManager
    participant ASM as AppStateManager
    participant GUF as GuideUIForm
    participant NWG as NetWorkGuide
    participant UH as UnityHandler

    CR->>EFM: ExecuteBackToLogon(Goodbye)
    EFM->>ASM: SetGoodByeing(true)
    EFM->>GUF: CloseGuideUIAndTask(false)
    EFM->>EFM: await Task.Delay(2000)
    EFM->>EFM: MainUIForm.GoBack()
    EFM->>EFM: BackToLogon(reason)
    EFM->>UH: PlayAnimOld("YT_tc_laoyangtuo")
    EFM->>EFM: WaitAndMoveOut()
    EFM->>ASM: SetGuideMode(Manual)
    
    Note over EFM: 异步等待 playerGetOutTime
    EFM->>NWG: Finish()
    EFM->>UH: PlayAnimOld("YT_yddh-02(57-86)")
```

### 关键配置参数

| 参数 | 配置路径 | 说明 |
|------|----------|------|
| `playerGetOutTime` | `JsonManager.Instance.playerData.PlayerConfigs[id].playerGetOutTime` | 退出动画等待时间（毫秒） |
| `ChargeStationId` | `JsonManager.Instance.ChargeStationId` | 充电桩ID |
| `isWakeupEnabled` | `AIKit.VoiceWakeup.isWakeupEnabled` | 语音唤醒开关 |

---

## 七、WebSocket 通信协议

### 发送指令 (DaoLanEvent)

| 事件 | 值 | 说明 | 触发方法 |
|------|-----|------|----------|
| Move | 2 | 开始移动 | `Move()` |
| Pause | 3 | 暂停移动 | `Pause()` |
| GotoPoint | 4 | 前往指定点位 | `GotoPointPur(stationId)` |
| AutoDaoLan | 5 | 自动导览 | `AutoDaoLan()` |
| AutoGlobalDaoLan | 6 | 全局导览 | `AutoGlobalDaoLan()` |
| GetDirection | 7 | 获取方向 | `GetDirection()` |
| HandDaoLan | 9 | 手动导览 | `HandDaoLan()` |
| Finish | 10 | 结束导览 | `Finish()` |
| NextPoint | 11 | 下一点位 | `NextPoint()` |
| FrontPoint | 12 | 上一点位 | `FrontPoint()` |
| GetIsChargeStation | 13 | 获取充电状态 | `GetIsChargeStation()` |

### 接收响应 (GuideResponse.event)

| 事件 | 值 | 说明 | 处理方法 |
|------|-----|------|----------|
| 导览错误 | 0 | 显示错误界面 | `HandleGuideError()` |
| 错误恢复 | 1 | 关闭错误界面 | `HandleGuideErrorRecovery()` |
| 方向判断结果 | 7 | 处理移动方向 | `HandleDirectionResult()` |
| 到达点位 | 8 | 处理到达事件 | `HandleArriveStation()` |
| 切换手动导览 | 9 | 模式切换通知 | 日志记录 |
| 充电状态变化 | 13 | 处理充电状态 | `HandleChargingStateChange()` |

---

## 八、语音指令路由

CommandRouter 支持的语音指令映射。

| 中文指令 | 英文指令 | 执行动作 |
|----------|----------|----------|
| 继续 | goon | `HandleContinue()` |
| 介绍上一点位 | introducethepreviouspoint | `NetWorkGuide.FrontPoint()` |
| 导航到上一点位 | navigatetopreviouspoint | `NetWorkGuide.FrontPoint()` |
| 介绍下一点位 | introducethenextpoint | `NetWorkGuide.NextPoint()` |
| 导航到下一点位 | navigatetonextpoint | `NetWorkGuide.NextPoint()` |
| 再见 | goodbye | `ExitFlowManager.ExecuteBackToLogon()` |
| 切换自动导览 | toggleautoguide | `NetWorkGuide.AutoDaoLan()` |
| 全局导览 | globalguide | `NetWorkGuide.AutoGlobalDaoLan()` |
| 切换手动导览 | togglemanualguide | `NetWorkGuide.HandDaoLan()` |

---

## 九、关键代码引用

### 核心管理类

| 功能 | 文件位置 | 说明 |
|------|----------|------|
| **状态管理** | | |
| 状态转换 | `AppStateManager.cs:43` - `TransitionTo()` | 切换应用状态 |
| 设置导航状态 | `AppStateManager.cs:52` - `SetNavigating()` | 设置IsNavigating标志 |
| 设置告别状态 | `AppStateManager.cs:60` - `SetGoodByeing()` | 设置IsGoodByeing标志 |
| 设置导览模式 | `AppStateManager.cs:67` - `SetGuideMode()` | 切换导览模式 |
| **导览管理** | | |
| 导览响应主入口 | `GuideMgr.cs:68` - `OnGuideResponse()` | 处理服务器响应 |
| 方向判断处理 | `GuideMgr.cs:108` - `HandleDirectionResult()` | event=7方向判断 |
| 向右移动 | `GuideMgr.cs:121` - `HandleMoveRight()` | direction=0/1时移动 |
| 到达终点 | `GuideMgr.cs:131` - `HandleReachDestination()` | direction=2到达终点 |
| 到达点位处理 | `GuideMgr.cs:140` - `HandleArriveStation()` | event=8点位到达 |
| 到达普通点位 | `GuideMgr.cs:214` - `HandleArriveNormalStation()` | 打开导览UI |
| 打开导览UI | `GuideMgr.cs:265` - `OpenGuideUI()` | 显示点位介绍 |
| **网络通信** | | |
| WebSocket连接 | `NetWorkGuide.cs:63` - `InitializeWebSocket()` | 建立WebSocket连接 |
| 移动指令 | `NetWorkGuide.cs:221` - `Move()` | event=2 |
| 暂停指令 | `NetWorkGuide.cs:231` - `Pause()` | event=3 |
| 结束指令 | `NetWorkGuide.cs:241` - `Finish()` | event=10 |
| 获取方向 | `NetWorkGuide.cs:252` - `GetDirection()` | event=7 |
| 前往点位 | `NetWorkGuide.cs:274` - `GotoPointPur()` | event=4 |
| 下一点位 | `NetWorkGuide.cs:286` - `NextPoint()` | event=11 |
| 上一点位 | `NetWorkGuide.cs:300` - `FrontPoint()` | event=12 |
| 手动导览 | `NetWorkGuide.cs:318` - `HandDaoLan()` | event=9 |
| 自动导览 | `NetWorkGuide.cs:331` - `AutoDaoLan()` | event=5 |

### UI播放系统

| 功能 | 文件位置 | 说明 |
|------|----------|------|
| 关闭UI和任务 | `GuideUIForm.cs:145` - `CloseGuideUIAndTask()` | 清理UI和取消任务 |
| 开始播放 | `GuideUIForm.cs:286` - `StartPlay()` | 初始化播放流程 |
| 主循环 | `GuideUIForm.cs:361` - `MainCoroutine()` | 遍历StageList播放 |
| 打字机效果 | `GuideUIForm.cs:470` - `WalkAsync()` | 文本逐字显示 |
| 音频播放 | `GuideUIForm.cs:506` - `PlayAudioWhenConditionAsync()` | 条件音频播放 |
| 视频播放 | `GuideUIForm.cs:630` - `PlayVideoWhenConditionAsync()` | 条件视频播放 |
| 图片播放 | `GuideUIForm.cs:709` - `PlayPicWhenConditionAsync()` | 条件图片播放 |

### 退出流程

| 功能 | 文件位置 | 说明 |
|------|----------|------|
| 退出流程入口 | `ExitFlowManager.cs:16` - `ExecuteBackToLogon()` | 根据原因执行退出 |
| 核心退出逻辑 | `ExitFlowManager.cs:35` - `BackToLogon()` | 统一退出处理 |
| 等待并移出 | `ExitFlowManager.cs:47` - `WaitAndMoveOut()` | 异步等待+Finish |

### 指令路由

| 功能 | 文件位置 | 说明 |
|------|----------|------|
| 指令检查 | `CommandRouter.cs:32` - `CheckInstruct()` | 语音指令→业务逻辑 |
| 再见指令 | `CommandRouter.cs:63` | 触发Goodbye退出 |
| 下一点位指令 | `CommandRouter.cs:57` | 调用NextPoint |
| 上一点位指令 | `CommandRouter.cs:49` | 调用FrontPoint |
| 自动导览指令 | `CommandRouter.cs:69` | 调用AutoDaoLan |
| 手动导览指令 | `CommandRouter.cs:81` | 调用HandDaoLan |

---

*文档由 Craft Agent 自动生成*
